const idade = 18

if (idade >= 18 && idade < 65 ) {
    console.log('Obrigado a votar')
} if (idade >= 16 && idade < 18) { // && = AND e || = OR
    console.log('Voto Opcional')
} if (idade < 16) {
    console.log('Não pode votar')
} if (idade >= 65) {
    console.log('Voto Opciona')
}
